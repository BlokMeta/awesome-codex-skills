# Heliograph — Agent Instructions

Read `CLAUDE.md` in this directory first; it is the canonical rule set (Codex and Claude share it). Then follow the document map in `docs/README.md`. Decisions live in `docs/adr/`, the live task list in `docs/12-teknik-yol-haritasi.md`.
